#pragma once

#include "BridgeCore.h"

// Register all clipboard related APIs
// clipboard.read, clipboard.write, clipboard.writeHTML, clipboard.writeFiles
/** @brief Register the clipboard.* API handlers. */
void RegisterClipboardApi();
