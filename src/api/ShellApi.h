#pragma once

#include "BridgeCore.h"

// Register all shell related APIs
// shell.showInExplorer, shell.openWith, shell.openExternal, shell.exec, shell.spawn
/** @brief Register the shell.* API handlers. */
void RegisterShellApi();
